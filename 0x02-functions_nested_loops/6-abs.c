#include "main.h"

/**
 * _abs - shows the absolute value
 *
 * @i: is an integer input
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
